<% for aliaz in @aliases %>
  <% if aliaz != @book.alias_title %>
    <p>
      <%= link_to h(aliaz.title), edit_book_alias_url(aliaz) %>
      <%= link_to image_tag('misc/edit.png'), edit_book_alias_url(aliaz) %>
      <%= link_to image_tag('misc/delete.png'), aliaz, :confirm => 'Are you sure you want to delete this alias?', :method => :delete %>
    </p>
  <% end %>
<% end %>

<% if @aliases.size < 2 %>
  <p>
    <i>No aliases have been added so far.</i>
  </p>
<% end %>
