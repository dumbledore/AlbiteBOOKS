<% if @book.ready %>
  <% subtitle = book_subtitle(@book, @show_author_name, @show_publication_date) %>
  <%= mobile_link_to_thumbnail_or_icon(book_url(@book), h(@title), subtitle, @book.thumbnail_url, 'book', true)%>
<% end %>