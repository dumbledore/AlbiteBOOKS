<% if @book.ready %>
  <% subtitle = book_subtitle(@book, @show_author_name, @show_publication_date) %>
  <%= mobile_link_to_thumbnail_or_icon(book_url(@book), h(@title), subtitle, h(freebase_thumb_url(@book.freebase_uid)), 'book', true)%>
<% end %>