<% subject_title = "Books in ‘#{h(@subject)}’" %>
<% title(subject_title) %>

<%= h1(h(subject_title)) %>
<%= render :partial => 'books/index' %>
<%= will_paginate @books, :renderer => 'WillPaginate::MobileRenderer' %>

<% content_for(:back) do %>
  <%= mobile_link_to(subjects_url, 'Subjects', 'Back to subjects', true, 'tag') %>
<% end %>